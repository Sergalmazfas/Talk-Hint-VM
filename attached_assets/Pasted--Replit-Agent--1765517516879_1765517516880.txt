Отлично. Тогда делаем чёткую, жёсткую задачу для Replit Agent, чтобы он НЕ импровизировал и НЕ лез в GPT как STT.

Ниже — ГОТОВЫЙ ТЕКСТ, который ты просто копируешь и вставляешь в Replit Agent.

⸻

✅ ЗАДАЧА ДЛЯ REPLIT AGENT (КОПИРОВАТЬ 1 В 1)

Контекст:
У нас уже полностью работает голосовой звонок через Twilio (входящие/исходящие).
Аудио передаётся корректно, ничего ломать НЕ НУЖНО.

Цель:
Добавить распознавание речи в реальном времени с помощью Deepgram.
GPT используется ТОЛЬКО ПОСЛЕ, для анализа уже распознанного текста.

⸻

❗ СТРОГИЕ ОГРАНИЧЕНИЯ (ОБЯЗАТЕЛЬНО)
	1.	❌ НЕ использовать OpenAI / GPT для распознавания речи
	2.	❌ НЕ менять существующую логику звонка (Dial, Voice URL, TwiML)
	3.	❌ НЕ использовать <Connect><Stream>
	4.	❌ НЕ добавлять запись звонков
	5.	❌ НЕ добавлять второй номер / callback / двойной набор
	6.	❌ НЕ трогать UI, если не требуется
	7.	❌ НЕ “оптимизировать” архитектуру

Работаем ТОЛЬКО с тем, что уже есть.

⸻

✅ ЧТО НУЖНО СДЕЛАТЬ

1️⃣ Использовать Twilio Media Streams
	•	Принимать аудио как есть
	•	Формат:
	•	audio/x-mulaw
	•	8000 Hz
	•	base64
	•	Треки:
	•	inbound — гость (Guest)
	•	outbound — хост (Owner)

⸻

2️⃣ Подключить Deepgram как STT
	•	Использовать Deepgram WebSocket API
	•	Передавать аудиофреймы напрямую из Twilio Media Stream
	•	Получать:
	•	partial transcripts
	•	final transcripts
	•	Обрабатывать inbound и outbound отдельно
	•	НИЧЕГО не возвращать в Twilio (только приём)

⸻

3️⃣ Логирование (обязательно)

В логах должно быть видно:

[Deepgram] inbound partial: ...
[Deepgram] inbound final: ...
[Deepgram] outbound partial: ...
[Deepgram] outbound final: ...

Без логов задача считается НЕ выполненной.

⸻

4️⃣ Переменные окружения

Я сам добавлю секреты, ты просто используй:

DEEPGRAM_API_KEY

❗ Никаких хардкодов.

⸻

🧱 АРХИТЕКТУРА (НЕ МЕНЯТЬ)

Twilio Call
   ↓
Media Stream (audio only)
   ↓
Deepgram (Speech-to-Text)
   ↓
Text (console / memory)
   ↓
(позже) GPT — отдельно


⸻

🎯 РЕЗУЛЬТАТ ЗАДАЧИ

Считается выполненной, если:
	•	Звонок работает как раньше
	•	Голос НЕ пропал
	•	В логах видно распознанную речь в реальном времени
	•	Deepgram используется как единственный STT
	•	GPT вообще НЕ трогается

⸻

⚠️ ВАЖНО

НЕ добавляй:
	•	записи
	•	playback
	•	TTS
	•	ответный звук
	•	двусторонний стрим

Только приём аудио → текст.

⸻

Если Replit Agent начинает:
	•	обсуждать OpenAI как STT
	•	добавлять <Connect><Stream>
	•	говорить про bidirectional audio
— это ошибка, откатывай изменения.

⸻

🟢 Готовность

Когда закончишь:
	•	ничего не спрашивай
	•	просто выложи изменения
	•	покажи логи с реальными фразами из звонка

⸻

Если хочешь, следующим шагом я могу:
	•	🔹 проверить, правильно ли он сделал
	•	🔹 дать короткий чеклист “принять/не принять PR”
	•	🔹 подготовить вторую задачу: GPT → подсказки

Скажи, что дальше.