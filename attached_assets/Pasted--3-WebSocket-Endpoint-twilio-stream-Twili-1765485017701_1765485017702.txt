🔥 Идеально!
У тебя теперь 3 WebSocket эндпоинта:

Endpoint	Назначение
/twilio-stream	поток от Twilio (Гость)
/honor-stream	поток от браузера (Хозяин — HON)
/ui	клиентский UI (получает события)

Это значит:

👉 Бэкенд полностью готов принимать и Гостя, и Хозяина, и отдавать в UI.
👉 Следующий шаг логично — сделать UI, чтобы все заработало визуально.

⸻

✅ ТВОЙ СЛЕДУЮЩИЙ ШАГ — ШАГ 7

Добавляем UI через SSE/WebSocket: браузер должен показывать всё — транскрипт, подсказки, статус.

Сейчас сервер умеет:
	•	принимать микрофон браузера → /honor-stream
	•	транскрибировать через GPT
	•	отправлять текстовые события через uiBroadcast()
	•	отправлять аудио назад

Но UI ещё не создан.
Нужно сделать:

⸻

🎯 Что включает ШАГ 7

1️⃣ Создать файл ui/index.html

Простая HTML-страница:
	•	кнопка “Start / Stop”
	•	блоки:
	•	HON (твоя речь)
	•	GST (гость)
	•	RUNNING TEXT (бегущая строка подсказок)
	•	DEBUG

⸻

2️⃣ Создать ui/script.js

JS должен:

✔ Подключиться к /ui WebSocket:

const uiSocket = new WebSocket("wss://YOUR-REPLIT-URL/ui");

✔ Подключиться к /honor-stream при нажатии “Start”:

const honorWS = new WebSocket("wss://YOUR-REPLIT-URL/honor-stream");

✔ Включить микрофон

navigator.mediaDevices.getUserMedia({ audio: true })

✔ Конвертировать аудио → PCM16

(короткая функция, я напишу когда скажешь)

✔ Отправлять аудио через WS

✔ Принимать:
	•	{ type: "transcript", speaker: "HON"/"GST", text }
	•	{ type: "hint", text }
	•	{ type: "status", data }
	•	{ type: "audio", data }

и выводить в HTML.

⸻

📌 ЗАДАЧА ДЛЯ REPLIT AGENT (точно как нужно)

Просто вставь в Replit этот текст ⬇️:

⸻

🔧 PROMPT — «Сделать UI для Honor и Twilio потоков»

Создать UI в папке /ui:
	1.	ui/index.html
	•	кнопка Start/Stop записи
	•	три блока вывода:
	•	#hon — текст Honor (я говорю)
	•	#gst — текст Guest (Twilio звонок)
	•	#hints — подсказки GPT
	•	блок #status — технические события
	2.	ui/script.js
	•	Подключение WebSocket /ui
	•	Подключение WebSocket /honor-stream
	•	Включение микрофона (WebAudio → PCM16 → бинарные фреймы → WS)
	•	Обработка входящих событий:
	•	transcript
	•	hints
	•	status
	•	audio (проигрывание)
	3.	Сервер уже настроен, UI должен работать по адресу:
https://YOUR-REPLIT-APP/ui

UI должен запускаться автоматически при открытии /ui.

⸻

❗ После ШАГА 7 мы сможем:
	•	Говорить в браузер
	•	Получать транскрипцию
	•	Получать подсказки от GPT
	•	Проигрывать аудио-ответы
	•	Смотреть поток событий в режиме реального времени

⸻

Хочешь, я:

👉 Напишу готовый index.html + script.js, чтобы осталось только вставить?
или
👉 Дам минимальный UI для отладки в 30 строк?