Отлично. Ты абсолютно точно сформулировал правильную точку старта.
Мы делаем как у взрослых продуктов: сначала Global Prompt (ядро поведения), потом всё остальное.

Ни папки, ни профессии НЕ ИМЕЮТ смысла, если нет правильно вшитого общего поведения GPT.

⸻

ШАГ 1 — ГЛОБАЛЬНЫЙ ПРОМПТ (ОБЯЗАТЕЛЬНЫЙ)

Это System Prompt, который:
	•	всегда подставляется первым
	•	не зависит от папок
	•	определяет КАК GPT себя ведёт

Он отвечает на вопрос:

«Ты кто, как ты думаешь, как ты говоришь и что ты делаешь в этом продукте»

⸻

✅ ГЛОБАЛЬНЫЙ PROMPT (готовый, можно вшивать)

Скопируй целиком, без изменений:

You are TalkHint — a real-time call coach and conversation assistant.

Your role:
- The user does NOT speak English fluently.
- The user needs EXACT phrases to say during live conversations.
- You do NOT teach.
- You do NOT explain grammar.
- You do NOT give long answers.

Your job:
- Give SHORT, READY-TO-SAY phrases.
- The user will READ them out loud.
- Always guide the conversation toward the USER'S GOAL.

Rules:
1. Be concise. Short sentences only.
2. Always suggest what the user should SAY next.
3. Do NOT explain why.
4. Do NOT ask unnecessary questions.
5. Assume the user is in a REAL conversation right now.
6. Respond immediately with usable phrases.

Language:
- Output phrases in the conversation language (usually English).
- ALWAYS include a translation into the user’s native language (RU or ES).

Format your responses like this:

SAY:
"Exact sentence to say"

TRANSLATION:
"Translation in the user's native language"

If multiple options are useful, provide 2–3 alternatives max.

You are NOT a chatbot.
You are a live call assistant.
Speed and clarity are critical.

👉 Это ядро всего продукта.
👉 Без него GPT всегда будет “умничать”, а не помогать.

⸻

ШАГ 2 — КАК ЭТО ВШИВАЕТСЯ В ПРОГРАММУ (ВАЖНО)

В коде каждый запрос к OpenAI должен выглядеть так:

Псевдологика:

messages = [
  { role: "system", content: GLOBAL_PROMPT },
  { role: "system", content: FOLDER_PROMPT_IF_EXISTS }, // позже
  ...conversationHistory
]

❗️Global Prompt всегда первый.

⸻

ШАГ 3 — ПРОВЕРКА (СРАЗУ ТЕСТ)

После вшивания, ты просто пишешь в чат:

“I need to call a doctor, get my test results and book an appointment.”

ОЖИДАЕМОЕ ПОВЕДЕНИЕ GPT:

Он НЕ спрашивает.
Он НЕ рассуждает.
Он СРАЗУ даёт:

SAY:
"Hello, I’m calling to ask about my test results."

TRANSLATION:
"Здравствуйте, я звоню, чтобы узнать результаты анализов."

Если он делает что-то другое — значит:
	•	либо промпт не вшит
	•	либо он не system
	•	либо он перезаписывается

⸻

ПОЧЕМУ МЫ ПОКА НЕ ДЕЛАЕМ ПАПКИ

Ты правильно сказал:

«Посмотрим, как это работает в истории»

Папка = дополнительный system prompt, но:
	•	если ядро плохое → папка не спасёт
	•	если ядро хорошее → папка = усилитель

⸻

ИТОГ: ЧТО ДЕЛАЕМ СЕЙЧАС

✅ ЗАДАЧА №1 (в работу)

Вшить Global System Prompt TalkHint
	•	добавить в backend
	•	проверить, что он всегда первый
	•	убедиться, что GPT отвечает “что сказать”, а не “как думать”

⸻

После этого (следующий шаг)

👉 Задача №2 — Folder Prompt (профессия как надстройка)

Но НЕ раньше, чем ты сам скажешь:

“Да, общее поведение GPT меня полностью устраивает”.

⸻

Если хочешь — следующим сообщением я:
	•	дам пример folder prompt (Doctor / Realtor)
	•	или помогу проверить, правильно ли агент реально вшил system prompt

Скажи: «Давай проверим» или «Давай папку».